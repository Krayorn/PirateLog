<!doctype html>
<html lang="fr">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/png" href="/favicon.png" />
		<meta name="generator" content={Astro.generator} />
		<title>Journal de Bord</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style is:global>
	@import url('https://fonts.googleapis.com/css2?family=MedievalSharp&family=Fondamento:ital@0;1&display=swap');

	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
		font-family: "MedievalSharp", serif;
		font-weight: 400;
		font-style: normal;
	}

	.font-medieval {
		font-family: "MedievalSharp", serif;
	}

	.font-fondamento {
		font-family: "Fondamento", serif;
	}

	.page_left {
		background-color: #e8d5a3;
		background-image:
			radial-gradient(ellipse at 15% 80%, rgba(139, 101, 56, 0.15) 0%, transparent 50%),
			radial-gradient(ellipse at 85% 20%, rgba(139, 101, 56, 0.1) 0%, transparent 40%);
		box-shadow:
			inset 20px 0 50px 25px rgba(160, 110, 50, 0.6),
			inset -3px 0 15px 5px rgba(139, 101, 56, 0.2),
			inset 0 5px 20px 5px rgba(139, 101, 56, 0.15),
			inset 0 -5px 20px 5px rgba(139, 101, 56, 0.15);
	}

	.page_right {
		background-color: #e8d5a3;
		background-image:
			radial-gradient(ellipse at 85% 80%, rgba(139, 101, 56, 0.15) 0%, transparent 50%),
			radial-gradient(ellipse at 15% 30%, rgba(139, 101, 56, 0.1) 0%, transparent 40%);
		box-shadow:
			inset -20px 0 50px 25px rgba(160, 110, 50, 0.6),
			inset 3px 0 15px 5px rgba(139, 101, 56, 0.2),
			inset 0 5px 20px 5px rgba(139, 101, 56, 0.15),
			inset 0 -5px 20px 5px rgba(139, 101, 56, 0.15);
	}

	.notch {
		clip-path: polygon(
			0% 0%,
			48% 0%,
			49% 5px,
			51% 5px,
			52% 0%,
			100% 0%,
			100% 100%,
			0% 100%
		);
	}

	.log-entry {
		margin: 0.3em 0;
		text-indent: 1.5em;
		color: #2c1810;
		font-size: 0.9rem;
		line-height: 1.55;
	}

	.nav-arrow {
		font-size: 2rem;
		color: #6b4c30;
		cursor: pointer;
		transition: color 0.2s ease, transform 0.2s ease, text-shadow 0.2s ease;
		user-select: none;
		z-index: 30;
	}

	.nav-arrow:hover {
		color: #3d2b1f;
		transform: scale(1.3);
		text-shadow: 0 0 8px rgba(196, 162, 97, 0.6);
	}

	/* ── Logbook tab: folded paper sticking out of the book ── */
	.logbook-tab {
		position: absolute;
		top: 20px;
		right: -12px;
		z-index: 25;
		cursor: pointer;
		width: 40px;
		height: 110px;
		background:
			linear-gradient(180deg, #f5e6c8 0%, #e8d5a3 40%, #dcc896 100%);
		box-shadow:
			2px 2px 8px rgba(0,0,0,0.35),
			inset -2px 0 6px rgba(139,101,56,0.2);
		border-radius: 0 4px 4px 0;
		display: flex;
		align-items: center;
		justify-content: center;
		transition: right 0.4s cubic-bezier(0.4, 0, 0.2, 1);
	}

	.logbook-tab::before {
		content: "";
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 12px;
		background: linear-gradient(180deg, rgba(139,101,56,0.25) 0%, transparent 100%);
		border-radius: 0 4px 0 0;
	}

	.logbook-tab::after {
		content: "";
		position: absolute;
		bottom: 0;
		left: 0;
		width: 100%;
		height: 12px;
		background: linear-gradient(0deg, rgba(139,101,56,0.25) 0%, transparent 100%);
		border-radius: 0 0 4px 0;
	}

	.logbook-tab:hover {
		right: -8px;
	}

	.logbook-tab-text {
		font-family: "MedievalSharp", serif;
		font-size: 0.8rem;
		color: #3d2b1f;
		writing-mode: vertical-rl;
		text-orientation: mixed;
		letter-spacing: 0.1em;
		text-shadow: 0 1px 0 rgba(255,255,255,0.3);
	}

	/* ── Logbook parchment: slides in from right, tilted like loose paper ── */
	.logbook-parchment {
		position: absolute;
		top: 4%;
		bottom: 10%;
		right: 20px;
		left: 30px;
		z-index: 20;
		pointer-events: none;
		transition: transform 0.5s cubic-bezier(0.4, 0, 0.2, 1);
		transform: translateX(110%);
	}

	.logbook-parchment.logbook-open {
		transform: translateX(0%) rotate(-1.2deg);
		pointer-events: auto;
	}

	.logbook-parchment.logbook-closed {
		transform: translateX(110%);
	}

	.logbook-parchment-inner {
		width: 100%;
		height: 100%;
		display: flex;
		flex-direction: column;
		overflow: hidden;
		background:
			linear-gradient(145deg,
				#faf0dc 0%,
				#f5e6c8 15%,
				#eedbb2 40%,
				#f0debc 65%,
				#e8d5a8 100%
			);
		box-shadow:
			-4px 4px 16px rgba(0,0,0,0.35),
			4px 6px 20px rgba(0,0,0,0.2),
			inset 6px 0 20px rgba(139,101,56,0.2),
			inset -4px 0 15px rgba(139,101,56,0.12),
			inset 0 6px 15px rgba(139,101,56,0.12),
			inset 0 -6px 15px rgba(139,101,56,0.12);
	}

	/* ── Pull-tab on the left edge to slide it back ── */
	.logbook-pull-tab {
		position: absolute;
		top: 50%;
		left: 0;
		transform: translateY(-50%);
		width: 22px;
		height: 60px;
		background:
			linear-gradient(180deg, #f5e6c8 0%, #e0c98e 100%);
		border-radius: 6px 0 0 6px;
		display: flex;
		align-items: center;
		justify-content: center;
		cursor: pointer;
		box-shadow: -3px 2px 6px rgba(0,0,0,0.25);
		transition: background 0.2s ease, left 0.2s ease;
	}

	.logbook-pull-tab span {
		font-size: 1rem;
		color: #6b4c30;
		transition: color 0.2s ease;
	}

	.logbook-pull-tab:hover {
		background: linear-gradient(180deg, #eedbb2 0%, #d4b876 100%);
		left: -4px;
	}

        .logbook-pull-tab:hover span {
                color: #3d2b1f;
        }

        .journal-stage {
                display: flex;
                align-items: center;
                justify-content: center;
                height: clamp(600px, 85vh, 900px);
                padding: 2rem 0 1.5rem;
                position: relative;
        }

        .journal-panel {
                width: 100%;
                height: 100%;
                display: flex;
                align-items: center;
                justify-content: center;
        }

        .journal-book {
                box-shadow: 0 18px 45px rgba(0,0,0,0.35);
        }

        .route-map-section {
                width: min(1200px, 92vw);
                margin: 2rem auto 3.5rem;
                padding: 0 1rem;
                color: #f2e5c9;
        }

        .route-map-header {
                display: flex;
                flex-direction: column;
                align-items: flex-start;
                margin-bottom: 1rem;
        }

        .route-map-title {
                margin: 0;
                font-size: 1.6rem;
                font-family: "MedievalSharp", serif;
                letter-spacing: 0.08em;
                color: #f1d6a2;
        }

        .route-map-subtitle {
                margin: 0.35rem 0 0;
                font-family: "Fondamento", serif;
                color: #d8c2a0;
                font-size: 1rem;
        }

        .route-map-frame {
                position: relative;
                border-radius: 18px;
                overflow: hidden;
                border: 1px solid rgba(245,230,200,0.35);
                box-shadow: 0 18px 45px rgba(0,0,0,0.45);
                background: #0f1a24;
                height: clamp(320px, 50vw, 550px);
        }

        .route-tooltip {
                font-family: "Fondamento", serif;
                font-size: 0.75rem;
                color: #3d2b1f;
                background: rgba(240,224,196,0.9);
                border: 1px solid rgba(196,162,101,0.5);
                box-shadow: 0 3px 8px rgba(0,0,0,0.3);
                padding: 0.15rem 0.5rem;
                border-radius: 4px;
        }

        @media (max-width: 900px) {
                .route-map-section {
                        margin-bottom: 2.5rem;
                }
        }
</style>
